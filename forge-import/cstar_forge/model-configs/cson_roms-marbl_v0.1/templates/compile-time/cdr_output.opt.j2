
      !-----------------------------------------------------------
      ! User inputs for cdr output module
      !
      !-----------------------------------------------------------

      logical,public,parameter :: do_cdr = {{ cdr_output.do_cdr|lower }}
      logical,parameter        :: do_avg = {{ cdr_output.do_avg|lower }}  ! NOTE: For most applications it is recommended that do_avg = .true.
      logical,parameter        :: monthly_averages = {{ cdr_output.monthly_averages|lower }} ! This overrides output_period
      real   ,parameter :: output_period = {{ cdr_output.output_period }} ! in seconds
      integer,parameter :: nrpf   = {{ cdr_output.nrpf }}          ! number of frames per file

      !
      !-----------------------------------------------------------
