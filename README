For now, switch to your conda base environment (run conda install conda-build if needed) and run

     `conda-build conda_recipe`

Then create a custome env for cstar with

     `conda create -n cstar_env python=3.12;`
     `conda activate cstar_env`

Before running

     `conda install --use-local cstar_ocean`
