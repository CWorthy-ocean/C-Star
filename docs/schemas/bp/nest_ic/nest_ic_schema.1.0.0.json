{
    "$defs": {
        "BlueprintState": {
            "description": "The allowed states for a work plan.",
            "enum": [
                "notset",
                "draft",
                "validated"
            ],
            "title": "BlueprintState",
            "type": "string"
        }
    },
    "additionalProperties": false,
    "description": "A blueprint for triggering a nested child simulation using the parent\nsimulation's outputs to generate initial conditions.",
    "properties": {
        "name": {
            "description": "A unique, user-friendly name for this blueprint.",
            "minLength": 1,
            "title": "Name",
            "type": "string"
        },
        "description": {
            "description": "A user-friendly description of the scenario to be executed by the blueprint.",
            "minLength": 1,
            "title": "Description",
            "type": "string"
        },
        "application": {
            "default": "nest_ic",
            "description": "The application identifier.",
            "title": "Application",
            "type": "string"
        },
        "state": {
            "$ref": "#/$defs/BlueprintState",
            "default": "notset",
            "description": "The current validation status of the blueprint."
        },
        "schema_version": {
            "default": "1.0.0",
            "description": "The schema version for the document.",
            "title": "Schema Version",
            "type": "string"
        },
        "working_dir": {
            "default": ".",
            "description": "Path to a directory where assets are stored when executing the blueprint.",
            "format": "path",
            "title": "Working Dir",
            "type": "string"
        },
        "parent_rst": {
            "description": "Path to a netCDF file containing initial conditions for the nested simulation.",
            "format": "path",
            "title": "Parent Rst",
            "type": "string"
        },
        "parent_grid": {
            "description": "Path to a netCDF file defining the parent grid attributes.",
            "format": "path",
            "title": "Parent Grid",
            "type": "string"
        },
        "child_grid": {
            "description": "Path to a netCDF file defining the child grid attributes.",
            "format": "path",
            "title": "Child Grid",
            "type": "string"
        },
        "pio": {
            "default": true,
            "description": "Whether the target ROMS build reads its inputs via ParallelIO, which requires\nCDF-5 (``NETCDF3_64BIT_DATA``) files. When True (the default), the initial\nconditions are written in roms-tools' default NETCDF4 format (fast) under an\n``_nc4``-suffixed name, then converted to CDF-5 at the intended filename via an\n``nccopy -k cdf5`` subprocess instead of roms-tools' native (much slower at\nscale) CDF-5 writer. When False, roms-tools' default save is used unchanged.",
            "title": "Pio",
            "type": "boolean"
        }
    },
    "required": [
        "name",
        "description",
        "parent_rst",
        "parent_grid",
        "child_grid"
    ],
    "title": "NestIcBlueprint",
    "type": "object"
}
