{
    "$defs": {
        "BlueprintState": {
            "description": "The allowed states for a work plan.",
            "enum": [
                "notset",
                "draft",
                "validated"
            ],
            "title": "BlueprintState",
            "type": "string"
        }
    },
    "additionalProperties": false,
    "description": "A blueprint used to perform upscaling of a high resolution nested grid to a lower resolution parent grid.",
    "properties": {
        "name": {
            "description": "A unique, user-friendly name for this blueprint.",
            "minLength": 1,
            "title": "Name",
            "type": "string"
        },
        "description": {
            "description": "A user-friendly description of the scenario to be executed by the blueprint.",
            "minLength": 1,
            "title": "Description",
            "type": "string"
        },
        "application": {
            "default": "upscaler",
            "description": "The application identifier.",
            "title": "Application",
            "type": "string"
        },
        "state": {
            "$ref": "#/$defs/BlueprintState",
            "default": "notset",
            "description": "The current validation status of the blueprint."
        },
        "schema_version": {
            "default": "1.0.0",
            "description": "The schema version for the document.",
            "title": "Schema Version",
            "type": "string"
        },
        "working_dir": {
            "default": ".",
            "description": "Path to a directory where assets are stored when executing the blueprint.",
            "format": "path",
            "title": "Working Dir",
            "type": "string"
        },
        "uscl_file_location": {
            "description": "Path to the .uscl file.",
            "title": "Uscl File Location",
            "type": "string"
        },
        "pio": {
            "default": true,
            "description": "Whether the target ROMS build reads its inputs via ParallelIO, which requires\nCDF-5 (``NETCDF3_64BIT_DATA``) files. When True (the default), the CDR forcing\nfile is written in xarray's default NETCDF4 format (fast) under an\n``_nc4``-suffixed name, then converted to CDF-5 at the intended filename via an\n``nccopy -k cdf5`` subprocess. When False, the NETCDF4 file is written directly.",
            "title": "Pio",
            "type": "boolean"
        }
    },
    "required": [
        "name",
        "description",
        "uscl_file_location"
    ],
    "title": "UpscalerBlueprint",
    "type": "object"
}
