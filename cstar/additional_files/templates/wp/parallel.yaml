# yaml-language-server: $schema=file://workplan-schema.json
name: Parallel Workplan
description: A workplan that executes all tasks in parallel
steps:
- name: Step A_1
  application: {application}
  blueprint: {blueprint_path}
- name: Step A_2
  application: {application}
  blueprint: {blueprint_path}
- name: Step A_3
  application: {application}
  blueprint: {blueprint_path}
state: draft
