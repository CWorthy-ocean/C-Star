name: cstar_env
channels:
  - conda-forge
dependencies:
  - jupyterlab
  - numpy>2.0,<2.4  # currently numba doesn't support 2.4
  - xarray
  - roms-tools>=3.5,<4
  - netCDF4
  - pip
  - ipykernel
  # only needed for building docs
  - pandoc
